// Side-effect bundle of the original Leaflet plugins, preserved verbatim so the
// map behaves exactly like the legacy site. `globalL` runs first to publish the
// global `L` the plugins expect.
import './globalL';
import './smoothwheelzoom.js';
import './fullscreen.js';
import './leaflet-hash.js';
import './leaflet-coordinates.js';
